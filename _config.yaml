# Present for parity with GitHub Pages' expectations only. `.nojekyll` sits
# next to it and disables the Jekyll build entirely, so the site is served as
# plain static files.
#
# ANONYMITY: no `author`, no `email`, no `github_username`, no `url` pointing
# at a personal domain.
title: "ManTra"
description: "Anonymous project page for a double-blind submission."
theme: null
